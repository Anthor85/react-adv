export const NoLazy = () => {
  return (
    <div className="no-lazy">
      <h1>No Lazy Loading</h1>
      <p>This page does not use lazy loading.</p>
      <p>
        Lazy loading is a technique to load components only when they are
        needed, improving performance and reducing initial load time.
      </p>
    </div>
  );
};

export default NoLazy;
